/**
 * @deprecated — Dead code with broken references.
 * Referenced estimate.lineItems (capital I) which never existed in the entity schema.
 * The active line item editor is EstimateGroups.jsx which uses estimate.groups.
 * Kept as empty stub to prevent import errors if referenced elsewhere.
 */
export default function EstimateCanvas() {
  return null;
}