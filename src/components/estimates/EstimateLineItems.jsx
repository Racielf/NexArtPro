/**
 * @deprecated — Dead code.
 * The active line item editor is EstimateGroups.jsx (grouped model with estimate.groups).
 * This file used the legacy flat model (estimate.line_items) and is no longer imported anywhere.
 * Kept as empty stub to prevent import errors.
 */
export default function EstimateLineItems() {
  return null;
}